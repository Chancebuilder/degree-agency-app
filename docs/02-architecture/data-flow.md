# Data flow

Transcript file stays in the browser. Structured candidates are reviewed. Approved rows POST to `/wallet/assets`. Matching reads wallet + equivalency + policy. Simulator reads remaining slots + opportunities + tuition rules.
