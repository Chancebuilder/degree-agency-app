# Security baseline

Argon2id passwords, short-lived JWT, rotating hashed refresh tokens, purpose-split consent, server-side revalidation of client-parsed rows, no secrets in git, Actuator limited to health/info/metrics.
